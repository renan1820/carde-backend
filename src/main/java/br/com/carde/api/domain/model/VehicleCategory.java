package br.com.carde.api.domain.model;

/**
 * Valores em minúsculo para corresponder diretamente ao contrato Flutter
 * e ao CHECK constraint do banco — sem necessidade de converter.
 */
public enum VehicleCategory {
    car, motorcycle, truck, bus, racing, classic
}
