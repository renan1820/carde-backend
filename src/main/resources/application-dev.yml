spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/carde_dev
    username: carde
    password: carde123
    hikari:
      connection-timeout: 5000
      maximum-pool-size: 5
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    br.com.carde: DEBUG
    org.hibernate.SQL: DEBUG

carde:
  cdn:
    base-url: http://localhost:8080
